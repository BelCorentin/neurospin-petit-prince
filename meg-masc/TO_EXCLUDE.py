# Simple constants about which subjects are not yet ready to be decoded onto:

# At the moment, the subject 18 has an
# issue with its run 1 (not enough channels)
# Sub 23: no trigger from run 1-5
to_exclude = ['18', '23']
